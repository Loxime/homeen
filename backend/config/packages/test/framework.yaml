framework:
    test: true

    session:
        storage_factory_id: session.storage.factory.mock_file

    rate_limiter:
        user_login:
            policy: fixed_window
            limit: 1000
            interval: '1 minute'
